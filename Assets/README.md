# Assets

`iPod.blend` is the editable Blender 4.5.9 source model. `ipod-mesh.json` is its exported geometry and material data, consumed by SceneKit. `ClickPod.icns` is generated by `Source/make_icon.swift`. Runtime LCD text and button labels are rendered in Swift.

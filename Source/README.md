# Source

`ClickPod.swift` contains the native app. `make_icon.swift` draws the application icon. `build_model.py` creates and exports the Blender model. Run `bash build.sh` from the repository root.
